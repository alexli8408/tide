# Kustomize only allows references to files under the kustomization root;
# sibling components must be directories with their own kustomization.yaml.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../crd
  - ../rbac
  - ../manager
